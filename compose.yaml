services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: examen_sw1
      POSTGRES_USER: examen_sw1
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
