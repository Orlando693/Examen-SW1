## 1. Increment 1 - RelationalModel And RelationalMapper

- [x] 1.1 Record the approved separate relational metadata boundary, `JOINED` inheritance, `VARCHAR(255) + CHECK` enums, composition restrictions, fail-closed diagnostics, automatic relation indexes, and future `com.generated.app` generator default consistently in proposal, design, specs, and tasks before code is written.
- [x] 1.2 Create `@examen-sw1/relational-core` with framework-independent, serializable relational contracts for schemas, enums, tables, columns, keys, constraints, indexes, relations, provenance, and structured diagnostics; verify package exports and root workspace participation.
- [x] 1.3 Implement deterministic canonical input normalization, relational identifiers, naming/reserved-word/length/collision resolution, stable collection ordering, and relational invariant validation; verify repeated equivalent inputs yield byte-equivalent serialized relational models.
- [x] 1.4 Implement generation-boundary validation and `RelationalMapper` entry behavior using canonical validation without reading layout, persistence, frontend, backend, or realtime state; verify blocking UML and unsupported mapping cases produce structured diagnostics and no generatable partial output.
- [x] 1.5 Map classes, scalar attributes, approved identifier strategy, primitive/enum types, nullability, unique constraints, metadata-derived indexes, synthetic identity keys, and generated-name provenance; verify focused unit cases assert semantic relational structures.
- [x] 1.6 Map supported 1:1, 1:N, N:M, aggregation, composition, foreign keys, join tables, and constraints under the approved multiplicity rules; verify FK ownership, nullable state, cascades, composite keys, and stable relationship ordering.
- [x] 1.7 Map approved `JOINED` generalization and reject multiple inheritance, cycles, unsupported finite multiplicities, custom/void attributes, ambiguous composition, and all other declared unsupported cases; verify diagnostics identify source elements and mappings remain deterministic.
- [x] 1.8 Add exhaustive mapper unit, determinism, collision/reserved-name, malformed-model, and focused relational golden tests; run relational-core test, typecheck, lint, build, and root checks without adding Java requirements.

## 2. Increment 2 - Spring Generator Foundation

- [x] 2.1 Verify Java 21 availability with `java --version` and `javac --version`, record the exact version, and add only the approved `handlebars` dependency and necessary types; verify lockfile integrity and no unrelated dependency upgrade.
- [x] 2.2 Create `@examen-sw1/spring-generator`, its deterministic options/contracts, template registry, ordered file planner, safe output-root writer, manifest/hash utility, and package exports; verify path traversal, duplicate normalized paths, and non-deterministic order are rejected.
- [x] 2.3 Add Handlebars templates and version-controlled Gradle Wrapper assets for Java 21, Spring Boot 4.x, Gradle, Spring Web MVC, Spring Data JPA, Hibernate, Jakarta Validation, Jackson, PostgreSQL, and springdoc-openapi; verify template rendering and generated build/wrapper file structure without global Gradle.
- [x] 2.4 Generate relational enum types, JPA entities, inheritance annotations, associations, repositories, persistence mappings, and PostgreSQL configuration from `RelationalModel`; verify generated source semantically represents keys, constraints, types, enums, and relation ownership.
- [x] 2.5 Generate DTO/request/response models, mapping layer, application services, REST controllers, Bean Validation, stable error handling, Jackson time setup, and springdoc configuration; verify representative rendered files and no CASE frontend/NestJS/Prisma imports.
- [x] 2.6 Define generated API conventions for create/read/update/delete/list/count and approved relationship navigation while reserving CU-07 derived OpenAPI/Postman/Domain Manifest work; verify generated routes/query contracts are consistent and stable across equivalent inputs.
- [x] 2.7 Add generator semantic tests for paths, ordering, template helpers, entities, repositories, DTOs, services, controllers, validation, errors, configuration, Gradle files, wrapper assets, and repeated-output equality; run generator package checks and applicable root checks.

## 3. Increment 3 - Advanced CRUD And Generated-Project Harness

- [ ] 3.1 Complete generated CRUD list/count behavior with bounded pagination, allow-listed sorting, declared filtering/search capabilities, and relationship navigation; verify generated tests cover valid and invalid query controls without persistence-internal error exposure.
- [ ] 3.2 Create a known canonical UML fixture covering scalar types, identifiers, enum, 1:1, 1:N, N:M, approved composition, joined inheritance, and generation metadata; verify its mapper result using semantic assertions rather than snapshots alone.
- [ ] 3.3 Implement the isolated generated-project harness with temporary output roots, wrapper invocation, Java 21 prerequisite checks, bounded process failure reporting, deterministic cleanup, and no sleeps/retries/skips; verify it reports unavailable/mismatched toolchains explicitly.
- [ ] 3.4 Generate the known fixture twice, compare relative paths, content hashes, and deterministic manifest, then run the generated Gradle Wrapper compile/test lifecycle with Java 21; verify generated entity/repository/service/controller and Spring context behavior as appropriate.
- [ ] 3.5 Verify PostgreSQL configuration expectations and generated relationship persistence behavior through generated tests or a documented real PostgreSQL harness where required; verify no CASE Prisma schema, migration, persistence API, or realtime protocol changes were made.
- [ ] 3.6 Run all relevant mapper/generator/harness tests, root test/typecheck/lint/build, generated-project build/tests, OpenSpec strict change/main-spec validation, `git diff --check`, and dependency review; record exact outcomes, Java/Gradle versions, limitations, and blockers in CU-06 documentation.
- [ ] 3.7 Update CU-06 documentation, STATUS, and HANDOFF with actual implementation and verification only; prepare verification/acceptance evidence without archiving, committing, pushing, or starting CU-07.
