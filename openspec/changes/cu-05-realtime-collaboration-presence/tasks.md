## 1. Increment 1 - Realtime Foundation, Session Lifecycle, Auth And Presence

- [x] 1.1 Add NestJS 11-compatible Socket.IO platform packages and compatible frontend Socket.IO client packages; validate Node 24 peers, lockfile integrity, audit delta, and `.env.example` defaults without forced upgrades.
- [x] 1.2 Configure authenticated Socket.IO namespace/origin allow-list, non-wildcard local frontend origins, transport payload limit, safe handshake logging, and no token URL/query usage; test accepted and rejected origins.
- [x] 1.3 Extract one JWT-to-current-user authenticator for HTTP bearer and Socket.IO `auth.token`, preserving existing HTTP behavior and rejecting missing/malformed/expired/deleted-user tokens without internals.
- [x] 1.4 Add verified-expiration timers, per-socket lifecycle generation, active project/session fields, and generation-safe join/leave/switch/disconnect/expiry cleanup; test concurrent joins, switch-versus-leave, disconnect during authorization, and expiry cleanup.
- [x] 1.5 Create a fail-hard realtime integration harness using a real listening Nest/Fastify server, real Socket.IO clients, isolated `TEST_DATABASE_URL`, no `DATABASE_URL` fallback, safe fixture cleanup, serialized workers, deterministic clocks/barriers, and guaranteed socket/server teardown.
- [ ] 1.6 Add typed event/ack contracts and closed error/action matrix for join, leave, command, resync, presence, resource updates, revocation, expiry, stale states, limits, domain/semantic failure, CAS conflict, and internal uncertainty.
- [x] 1.7 Create `CollaborationModule`, transport-only Gateway, `ProjectMutationCoordinator`, atomic project-session registry, session lifecycle state, bounded queue registry, error mapper, rate component, and focused presence registry without a durable document cache.
- [ ] 1.8 Implement atomic `getOrCreate(projectId)` session creation with one UUID epoch, realtime version 0, one queue, dedupe state, generation-safe reconnect TTL eviction, and cancellation on rejoin; test concurrent first joins and join-versus-eviction.
- [x] 1.9 Add authoritative project loader/decoder for collaboration using the existing OWNER/EDITOR policy and concealment; test owner, editor, unrelated, pending invitation, ownerless, corrupt, and unsupported-schema cases against PostgreSQL.
- [x] 1.10 Implement generation-checked coordinator join/leave/switch/resync handshake with room membership, authoritative snapshot/digest/version capture, one active project per socket, and safe failed-switch behavior; test A-to-B success and denied B leaves no A subscription.
- [x] 1.11 Add minimal frontend collaboration client/controller lifecycle with auth-session token source, listener-before-join behavior, controller generation, bounded pre-install join buffer, disposal, and no duplicated socket/listener under Strict Mode.
- [x] 1.12 Implement join-buffer foundation rules: listener-before-join, bounded current-controller/project buffering, snapshot replacement of HTTP state, overflow-to-resync control flow, and clear on switch/disposal; test join/switch control flow with synthetic sequence-tagged event fixtures only, without durable UML command execution or delivery.
- [ ] 1.13 Implement protected per-emission recipient filtering for unexpired current OWNER/EDITOR access across snapshots, presence, resource updates, and future command events; test expiry/access loss immediately before emission.
- [x] 1.14 Implement complete authorized participant roster from owner plus accepted editor memberships with safe identity, derived email initials/avatar, online/offline state, nullable server-derived `lastActivityAt`, and exclusion of invitations/unrelated identities.
- [x] 1.15 Implement per-socket full presence snapshots, user-level online dedupe, deterministic most-recent-socket aggregation, and recomputation/clearing on socket loss; test two tabs with distinct state and final offline transition.
- [x] 1.16 Implement flow-space cursor publication using React Flow coordinate conversion and viewport-aware remote presentation, without raw screen coordinates or viewport mutation; test zoom/pan/viewport independence and pane-leave clearing.
- [x] 1.17 Implement bounded selected IDs, edited element, and allow-listed activity presence with server-derived timestamps and presentation-only remote overlays; test malformed/cross-project/deleted references and local-selection preservation.
- [x] 1.18 Implement client 20 Hz cursor coalescing, immediate selection/editing/activity publication, trailing-work cancellation, server 30 Hz bounded presence rate/burst, and payload limits; test fake-clock cleanup after leave, hidden tab, switch, unmount, and disconnect.
- [x] 1.19 Add Increment 1 real Socket.IO/PostgreSQL integration coverage for auth, rooms, atomic epochs, join buffering, switching, emission-time authorization/expiry, roster, online/offline, cursor, selection/editing, multi-tab, limits, and cleanup.

> Scope review (2026-09-13): 1.6, 1.8, and 1.13 remain unchecked. Their current auth/session/presence portions are implemented and evidenced by Increment 1, but each also requires later durable command and/or resource-update behavior. They remain in place without reclassification because each mixes Increment 1 and later-scope requirements.

## 2. Increment 2 - Authoritative Commands, CAS And Recovery

- [ ] 2.1 Add framework-independent strict runtime decoders for exactly the 19 existing UML commands and the realtime envelope including `baseRevision`; reject unknown/extra/authority/React Flow/document fields, invalid UUIDs/enums, non-finite values, and bounded array/string violations.
- [ ] 2.2 Add deterministic canonical command-intention/document serialization and SHA-256 digest utilities with sorted keys, preserved arrays, finite JSON values, materialized semantic defaults, and no `undefined`; test equivalent key order and semantic mismatch cases.
- [ ] 2.3 Implement pure `normalizeRealtimeCommand` against the loaded resource, materializing every executor-generated create/layout ID, defaults, authoritative timestamp, and validated `ApplyLayout` ordering; test all optional-ID command families and existing/new layout nodes.
- [ ] 2.4 Define and implement `ProjectCommandApplied` with bases/results, actor, normalized command, authoritative timestamp, storage version, document revision, and resulting durable document digest; ensure snapshots include matching full resource/digest.
- [ ] 2.5 Add a central frontend `submitRealtimeCommand()` gate used by every model/layout mutation path, generating one command ID and allowing exactly one in-flight command while keeping canonical document unchanged; test bypass attempts from toolbox, inspector, keyboard, relation, drag, and auto-layout paths.
- [ ] 2.6 Implement server command sequence in the project coordinator: lifecycle/auth/access check after queue wait, dedupe-before-stale, session/realtime validation, authoritative reload, base-revision validation, decode/normalize, command bus, semantic validation, and storage CAS.
- [ ] 2.7 Implement session-scoped dedupe keyed by `(projectId, sessionId, commandId)` using canonical intent digest/actor and bounded TTL/LRU cleanup; test lost-ACK retry as `DUPLICATE`, payload/actor mismatch, one durable write, and old epoch stale behavior.
- [ ] 2.8 Add exact-result authorization-aware command persistence CAS returning the exact durable resource; prove canonical revision advances only through `UmlCommandBus`, storage version advances once, and frontend never supplies final storage version.
- [ ] 2.9 Implement bounded CAS conflict classification: one reload/retry for metadata-only storage changes, epoch invalidation for canonical changes or retry exhaustion, and concealment for deletion/access loss; test every classification with deterministic barriers.
- [ ] 2.10 Declare and implement the durable commit point, then session version/dedupe/result finalization; inject post-CAS faults and prove poisoned epoch blocks old commands, resync recovers persisted state, and no false rollback occurs.
- [ ] 2.11 Implement sender-ack/others-broadcast delivery using acknowledgement for origin and room broadcast excluding origin; test no duplicate sender application and no broadcast for `DUPLICATE`.
- [ ] 2.12 Implement frontend generation-checked applied ingestion: apply next contiguous result once, ignore consumed old results, resync on gap/session/digest mismatch, and drop stale-controller callbacks; bound applied-event tracking and buffers; with real Socket.IO command delivery, test a `ProjectCommandApplied` event arriving after room join/snapshot capture but before frontend join-ACK processing.
- [ ] 2.13 Implement ack-timeout uncertainty handling: apply no local intent, enter resyncing, request authoritative snapshot, and never generate a new command ID or blind retry; test timeout followed by delayed event/ack.
- [ ] 2.14 Rebase `UmlHistory` with empty stacks after every authoritative join, own/remote applied command, resync, and collaboration exit; test no remote/pre-collaboration snapshot can undo shared work.
- [ ] 2.15 Route local drag commit only to `MoveNode`, keep cursor on presence channel, and apply normalized `ApplyLayout` once from originating ELK computation without rerunning ELK remotely; test no movement/presence cross-emission.
- [ ] 2.16 Add real deterministic command integration tests for all 19 command types, owner/editor propagation, canonical model/layout/digest convergence, validation failure, delete/edit, rename/delete, same-field, and same-base conflicts.
- [ ] 2.17 Add deterministic version tests proving separate `revision`, `storageVersion`, `realtimeVersion`, `sessionId`, and `documentSchemaVersion` behavior for commands, duplicates, rejections, resync, and presence.
- [ ] 2.18 Add real PostgreSQL recovery tests for persistence-before-APPLIED, storage version propagation, reopen, restart/session replacement, stale session/realtime/document bases, and no accepted-but-undurable window.

## 3. Increment 3 - External Mutation Coordination, UX And Hardening

- [ ] 3.1 Route `PUT /projects/:id/document` through `ProjectMutationCoordinator`, preserving its HTTP/CAS contract while invalidating active epochs, requiring participant resync/rejoin, and preventing old-epoch command application; test both deterministic arrival orders.
- [ ] 3.2 Route metadata PATCH through the coordinator, retain its storage-only version semantics, and emit protected `project:resource-updated` without changing canonical revision/realtime version; test PATCH versus command metadata-only retry.
- [ ] 3.3 Route DELETE through the coordinator, invalidate epoch, remove sockets/rooms/presence/timers/dedupe, and prevent recreation; test deterministic DELETE versus command ordering and post-delete concealment.
- [ ] 3.4 Finalize all stable error code/action mappings and safe messages, including schema/internal uncertainty behavior; test no stack/Prisma/SQL/JWT/token/concealed-version leakage.
- [ ] 3.5 Finalize socket limits/configuration: payload, command rate, presence rate, string/collection limits, layout hard maximum, join buffer, session/dedupe capacity, TTL, origin allow-list, environment defaults, and cleanup; test abuse boundaries without unbounded memory.
- [ ] 3.6 Integrate explicit web editor lifecycle/persistence states `connecting`, `joining`, `connected`, `resyncing`, `disconnected`, `auth-required`, and `error` with accessible Saving/Saved/Disconnected/Resynchronizing/Error status feedback.
- [ ] 3.7 Replace connected Save behavior with non-action collaboration persistence status, preserve HTTP Save API compatibility only outside normal realtime editor flow, and ensure disconnection never silently re-enables offline snapshot Save.
- [ ] 3.8 Disable Undo/Redo during active collaboration with accessible explanation, retain non-mutating viewport/inspection interactions while disconnected, and test all editor controls obey the central mutation gate.
- [ ] 3.9 Render responsive roster/avatar/online/offline/activity UI and remote cursor, selection, and editing overlays without replacing local selection or obscuring canvas; test desktop, tablet, mobile, and breakpoint transitions.
- [ ] 3.10 Enforce viewport policy: initial authoritative join may fit once; commands, resync, presence, and remote/local layout changes never auto-fit; explicit Fit View remains; test user-panned viewport preservation and no ResizeObserver/store loop.
- [ ] 3.11 Add React Flow regressions for remote-to-local command echo, maximum update depth, error `#004`, hydration, duplicate keys, stale listeners, coordinate alignment, and no direct React Flow domain mutation.
- [ ] 3.12 Evaluate Playwright only as development tooling; if adopted, automate minimum multi-context join/remote move/no-refit/disconnect/cursor checks, otherwise document repeatable manual Chrome evidence. Manual Chrome remains mandatory.
- [ ] 3.13 Prepare reproducible secure browser-acceptance setup for owner/editor/unrelated/two tabs/projects/network/expiry/access-loss cases without secrets or manually pre-created test data.

## 4. Final Verification And CU Documentation

- [ ] 4.1 Run all core/frontend/backend/root tests plus the fail-hard real PostgreSQL/Socket.IO suite; record exact outcomes and resolve relevant failures.
- [ ] 4.2 Run Prisma generate/validate and isolated migration validation, proving no CU-05 migration and compatibility of existing projects/auth/memberships.
- [ ] 4.3 Run typecheck, lint, production builds, `git diff --check`, and dependency/audit review; distinguish prior warnings from CU-05 blockers.
- [ ] 4.4 Execute deterministic real integration evidence for first joins, eviction, same base, dedupe lost-ACK, PUT/PATCH/DELETE races, access loss, JWT expiry queue/emission, and post-CAS poison recovery.
- [ ] 4.5 Execute real manual Chrome owner/editor acceptance for roster online/offline/avatar/activity, class/relationship/rename/multiplicity/move/layout, cursor/selection/editing, two-tab arbitration, disconnect/resync/reopen, and persistence without reload.
- [ ] 4.6 Execute manual Chrome security/recovery acceptance for auth expiration, unrelated concealment, switch denial isolation, blocked mutations, error handling, and console/network checks with no CORS, hydration, update-depth, React Flow #004, duplicate-key, or unhandled socket failures.
- [ ] 4.7 Update CU-05 use-case documentation, `docs/STATUS.md`, and `docs/HANDOFF.md` with actual implementation only, test evidence, limitations, single-instance debt, and next action; do not claim archive/commit/push before their workflows.
- [ ] 4.8 Run OpenSpec strict change/spec validation and verification readiness checks; leave archive, commit, and push pending implementation, verification, and explicit user acceptance.
