# Project Handoff

## CU activo

CU-05 - Colaboracion realtime y presencia. Incrementos 1 y 2 implementation-complete. 1.8 esta completa; 1.6 y 1.13 quedan abiertas solo por `resource updates` de Incremento 3. Incremento 3 no ha comenzado.

## Trabajo terminado

- CU-04 esta COMPLETE, VERIFIED, MANUALLY ACCEPTED y ARCHIVED en `openspec/changes/archive/2026-09-12-cu-04-auth-ownership-invitations`.
- Entrego auth JWT/Argon2id con `sessionStorage`, ownership/EDITOR authorization, IDOR concealment, authorization-aware CAS y lifecycle seguro de invitaciones email-bound con persistencia hash-only.
- Evidencia: root tests 133 PASS; migration history limpia de cinco migraciones PASS; Chrome manual owner/editor/unrelated/invitations PASS.
- CU-05: Presence frontend, lifecycle del cliente, join buffer bounded con overflow/resync y cursor coalescido estan implementados. La integracion real Socket.IO/PostgreSQL cubre roles, rooms, epoch concurrente, switching, expiry, roster, Presence bidireccional y cleanup offline.
- Aceptacion manual multi-profile CU-05 PASS: sesiones compartidas, roster, cursor bidireccional, pan/zoom, seleccion remota de nodo/relationship, editing/dragging Presence, hidden-tab cleanup, disconnect/reconnect sin duplicados y consolas limpias. RemoteSelectionOverlay usa bounds medidos actuales de React Flow (`useInternalNode`, `positionAbsolute`, `measured`), sin modificar viewport ni seleccion local.
- Task 2.1 completo sin conectar el decoder al gateway: `decodeRealtimeCommandEnvelope()` valida envelope cerrado y las 19 variantes `UmlCommand` con UUIDs, versions, enums, payloads anidados, limites y rechazo de authority/UI fields. Evidencia: decoder 32 PASS, `uml-core` 36 PASS, backend typecheck/lint/build PASS.
- Task 2.2 completo: serializacion canonical pura y SHA-256 hexadecimal para intent con project/session/actor/bases/command y `ProjectDocument` autoritativo. No normaliza, ejecuta, persiste ni emite. Evidencia digest/decoder 36 PASS, `uml-core` 36 PASS, backend typecheck/lint/build PASS.
- Task 2.3 completo: `normalizeRealtimeCommand()` puro recibe recurso cargado, IDs server-selected y timestamp caller-supplied; materializa IDs/defaults y valida ApplyLayout existente/nuevo/duplicado conservando orden. No ejecuta, persiste, avanza versiones ni integra gateway. Evidencia foundation 58 PASS, `uml-core` 36 PASS, backend typecheck/lint/build PASS.
- Task 2.4 completo: `ProjectCommandApplied` y snapshot de recurso autoritativo derivan revision/storage/digest desde fixture/resulting resource, preservando bases/realtime/actor/commandId como inputs. Join/resync incluye digest de resource. Sin handler, executor, persistence/CAS, ACK o broadcast. Evidencia foundation 60 PASS, integration 15 PASS, `uml-core` 36 PASS, backend typecheck/lint/build PASS.
- Tasks 2.6-2.10 completas: `ProjectCommandCoordinator` es la ruta backend serializada para un envelope estricto ya unido. Tras revalidar auth/acceso hace dedupe actor+intent antes de precondiciones stale, recarga PostgreSQL, normaliza, usa `UmlCommandBus`, valida y ejecuta CAS authorization-aware que devuelve la fila durable exacta. Un retry CAS maximo admite solo cambios de metadata; cambio canonico o segundo conflicto invalida epoch. El CAS es commit point; post-CAS hook para test envenena epoch sin rollback, bloquea comandos antiguos y resync relee PostgreSQL. Dedupe TTL/LRU bounded permanece por sesion.
- Task 2.11 completa: el gateway `project:command` devuelve ACK al originador solo despues de persistencia y emite `project:command-applied` a los demas participantes autorizados, excluyendo el socket originador. `DUPLICATE` devuelve su resultado previo sin nuevo broadcast. Evidencia: backend 135 PASS; integracion real PostgreSQL/Socket.IO 22 PASS; typecheck/lint/build PASS.
- Task 2.5 completa: `RealtimeCommandGate` crea el envelope tipado `project:command` desde los metadatos actuales del bridge, genera un UUID, limita a un pending y descarta callbacks de generaciones limpiadas. `UmlEditorClient` instala el gate solo tras join y lo limpia en disconnect, expiry, switch/unmount. Las 19 variantes `UmlCommand` del store pasan por el gate en realtime y preservan la ejecucion/historial local fuera de realtime. No hay mutacion optimista de documento, history, revision, storageVersion ni snapshot antes o despues de ACK; Task 2.12 sera la unica ingestion. Evidencia frontend: 141 tests PASS y typecheck PASS.
- Task 2.12: `AuthoritativeCommandIngestionController` valida baseline generation/project/session/realtime/revision/storage/resource, serializa eventos, clasifica `NEXT`/`OLD`/`GAP`/`DIFFERENT_SESSION`/`STALE_GENERATION`/`MISMATCH`, ejecuta solo el comando normalizado contra candidato mediante `UmlCommandBus` y comprueba revision/digest SHA-256 Web Crypto. El bridge usa esta unica instancia para snapshots join/resync, ACK y broadcast; instala el store solo desde la baseline validada y coalesce recovery. El harness real Nest/Fastify/Socket.IO/PostgreSQL instancia `CollaborationClient` y bridge reales y retrasa solo la resolucion del ACK de join: el broadcast v1 de otro socket llega con listener activo despues del snapshot v0 y antes de su instalacion, se bufferiza, snapshot se instala primero y el replay ocurre una vez con versiones y digest verificadas. La regresion frontend descarta el duplicate ya cubierto por un snapshot. Backend realtime 23 PASS; frontend 151 PASS; raiz 323 PASS; typecheck/lint/build de ambos workspaces y raiz PASS.

## Aceptacion manual reciente

- Evidencia durable aportada por usuario: OWNER/EDITOR PASS para crear, renombrar, mover y agregar atributo en realtime sin Save ni F5. El diagnostico esperado para una clase en minusculas fue observado.
- Task 2.13 completa: cada comando pending tiene timeout por generacion. El timeout no aplica intencion ni reintenta/genera `commandId`; mantiene el gate bloqueado, pone el editor en `resyncing` y solicita recovery coalescido al bridge. Solo la instalacion exitosa del snapshot autoritativo libera el gate. ACK tardio durante recovery se ingiere sin adelantar esa liberacion; posterior al snapshot se descarta porque este es la autoridad. `clear` cancela timers/callbacks stale. Evidencia: root 326 PASS (frontend 154, backend 136, core 36), typecheck/lint raiz PASS y build frontend PASS.
- Task 2.14 completa: `installAuthoritativeDocument` rebasa `UmlHistory` con stacks vacios para join, ACK propio, broadcast remoto y resync; la salida de una colaboracion ya unida vuelve a basar historial en el documento autoritativo actual. Undo/Redo y `Ctrl/Cmd+Z`/`Ctrl/Cmd+Shift+Z` siguen disponibles fuera de realtime y quedan bloqueados, incluidos controles, mientras existe la compuerta realtime. Evidencia: frontend 156 PASS; typecheck/lint/build frontend PASS; OpenSpec strict PASS.
- Task 2.15 completa: el inicio y frames de drag permanecen en presencia, y solo el commit de drag envia un `MoveNode` flow-space por el gate, sin mutacion canonica ni historial local hasta ingestion autoritativa. Auto-layout envia un unico `ApplyLayout` calculado por ELK, sin `MoveNode`, optimismo ni ELK remoto. Cambios de layout autoritativos no ejecutan `fitView` ni realimentan comandos; el flujo local previo se conserva. Evidencia: frontend 159 PASS; typecheck/lint/build frontend PASS; OpenSpec strict PASS y `git diff --check` PASS con warnings LF/CRLF conocidos.
- Tasks 2.16-2.18 completas: el harness real Nest/Fastify/Socket.IO/PostgreSQL prueba las 19 variantes de comando, propagacion OWNER/EDITOR, convergencia modelo/layout/digest, fallos semanticos, target eliminado y conflictos. Prueba los cinco dominios de version en command/duplicate/rejection/resync/presence y persistence-before-APPLIED, reapertura tras reemplazo de epoch y sesion vieja stale. El manager tiene prueba fake-clock de cancelacion de eviction/rejoin. Backend focalizado: 28 PASS, typecheck/lint PASS.

## Pendiente

- Incremento 3 pendiente. No archivar, push ni declarar CU-05 cerrado sin sus tareas, verificacion y aceptacion final. 1.6/1.13 permanecen unchecked solo por `project:resource-updated`; 1.8 esta marcada completa.
- CU-04 permanece pendiente de commit y push por instruccion explicita del usuario.

## Limitaciones

- Playwright no esta instalado; la evidencia de navegador de CU-04 es manual Chrome suministrada por el usuario.
- El backend acepta y entrega comandos durables y el editor ya los envia sin optimismo, pero todavia no ingiere ACK/broadcast autoritativos; por tanto no puede avanzar la proyeccion ni encadenar comandos desde versiones nuevas hasta Task 2.12. CU-05 conserva `baseRevision` y `baseRealtimeVersion` separados de `storageVersion`, session epochs efimeros, presencia sin migracion y un unico proceso.

## Siguiente accion exacta

Esperar instruccion para iniciar Incremento 3. El usuario solicitó commit del cierre de Incremento 2, sin push ni archive.
