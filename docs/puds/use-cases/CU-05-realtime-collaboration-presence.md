# CU-05 - Realtime Collaboration And Presence

## Objective

Allow authorized project owners and editors to collaborate through Socket.IO while PostgreSQL remains the durable authority and `ProjectDocument` remains the canonical UML source of truth.

## Scope and Dependencies

CU-05 builds on persisted projects, JWT authentication, ownership, and editor memberships from CU-03 and CU-04. It does not add CRDT/OT, distributed coordination, external mutation coordination, or a persistent command log.

## Increment 1

Increment 1 implemented authenticated rooms, session lifecycle, PostgreSQL-backed authoritative join/resync snapshots, ephemeral roster/presence, flow-space cursors, bounded client/server presence updates, and frontend lifecycle buffering. Owner/editor manual acceptance was supplied by the user.

## Increment 2 Implementation

- The backend strictly decodes and normalizes all 19 supported `UmlCommand` variants, then executes them only with `UmlCommandBus`.
- The command coordinator serializes per-project work, deduplicates canonical intents, validates session/realtime/document bases, persists via authorization-aware CAS, and treats the returned CAS row as the durable commit point.
- Applied results include normalized commands, distinct revisions, storage/realtime versions, server timestamp, and canonical SHA-256 document digest. Sender ACK and authorized collaborator broadcast use the same result.
- The frontend command gate is non-optimistic. The authoritative ingestion controller installs snapshots and applies only contiguous verified results, with bounded recovery on timeout, gaps, stale generations, or digest mismatch.
- Authoritative installations rebase local history. Realtime drag commits one `MoveNode`; auto-layout sends one `ApplyLayout` and never reruns ELK remotely.

## Decisions

- `ProjectDocument.model` is semantic authority, `ProjectDocument.layout` is visual persisted state, and React Flow remains a projection.
- `revision`, `storageVersion`, `realtimeVersion`, `sessionId`, and `documentSchemaVersion` are separate domains.
- Accepted commands are persisted before acknowledgement or broadcast. No optimistic canonical mutation, blind retry, or client-selected storage version is allowed.
- The implementation targets one backend process. Session state, dedupe, presence, and queues are ephemeral.

## Automated Evidence

- `backend/test/realtime-collaboration.integration.spec.ts` uses real Nest/Fastify, Socket.IO clients, and isolated PostgreSQL. It now covers all 19 command types, owner/editor propagation, canonical model/layout/digest convergence, validation and target failures, same-base conflicts, dedupe, version domains, durable commit-before-APPLIED, and epoch replacement/reopen recovery.
- `backend/src/collaboration/collaboration-session.manager.spec.ts` uses a fake clock to verify reconnect eviction cancellation and old-timer safety.
- Increment 3 Task 3.1 routes HTTP `PUT /projects/:id/document` through the same per-project `ProjectMutationCoordinator` as joins and commands. It preserves the existing validation, OWNER/EDITOR concealment, revision, and storage-version CAS contract; only a successful replacement invalidates the captured collaboration epoch, so its old commands return `STALE_SESSION` and a resync creates a new epoch from PostgreSQL.
- `backend/src/collaboration/project-mutation-coordinator.spec.ts` proves a failed queued operation releases its project queue and a blocked project does not block another project. `backend/test/realtime-collaboration.integration.spec.ts` uses real Fastify HTTP, Socket.IO, and isolated PostgreSQL barriers to prove command-before-PUT retains the established HTTP `409 PROJECT_REVISION_CONFLICT`, while PUT-before-command persists the replacement, invalidates the old epoch, rejects the queued command as `STALE_SESSION`, and restores the replacement only after resync. Focused realtime integration: 30 passed; backend typecheck and lint passed.
- Increment 3 Task 3.4 centralizes the closed Socket.IO error/action matrix, including `SCHEMA_INCOMPATIBLE`, `INTERNAL_STATE_UNCERTAIN`, and `INTERNAL_ERROR`. Its mapper tests prove all public messages/actions and reject exception text containing stack, Prisma, SQL, JWT, token, or version data; the real Socket.IO/PostgreSQL suite verifies unsupported schema acknowledgement has no concealed schema-version leakage. Backend test: 141 passed; backend and frontend typecheck, lint, and production build passed; strict OpenSpec validation passed.
- Increment 3 Task 3.2 serializes metadata PATCH in the same coordinator. Its owner-only CAS returns the exact durable row, which becomes a full-resource SHA-256 snapshot in `project:resource-updated`; it changes only storage version and project metadata, never canonical revision or realtime version. The gateway revalidates each recipient immediately before this emission. The frontend checks project/session/revision/storage progression and digest before installing it; old epochs are ignored and canonical/storage gaps trigger one coalesced authoritative resync. Real Fastify/Socket.IO/PostgreSQL tests prove owner/editor delivery of the exact returned resource, PATCH-before-command ordering with storage version two after the command, and access revocation immediately before emission. Root tests: 345 passed (frontend 161, backend 148, UML core 36); typecheck, lint, and both production builds passed.
- Increment 3 Task 3.3 routes owner DELETE through that same coordinator. After its owner/version CAS commits, `terminateProject()` removes the project session, dedupe map, eviction timer, presence, per-socket rate state, rooms, active socket state and expiry timers, then sends only the generic `project:revoked` failure before disconnecting affected clients. The deleted row remains authoritative, so later joins and commands cannot recreate an epoch or disclose project state. Deterministic real Fastify/Socket.IO/PostgreSQL barriers cover command-before-DELETE (`409 PROJECT_REVISION_CONFLICT`) and DELETE-before-command (`PROJECT_NOT_FOUND`/`LEAVE`), terminal socket disconnect, no session/row remaining, HTTP concealment, and denied later join. Focused cleanup tests: 26 passed; backend integration: 51 passed; full backend: 153 passed; backend typecheck, lint, build and strict OpenSpec validation passed.
- Increment 3 Task 3.5 centralizes transport configuration in `collaboration-limits.ts`. Socket.IO and Fastify use the same 1 MiB payload maximum and explicit non-wildcard `FRONTEND_ORIGIN` allow-list; strict command decoding/normalization use the configured 256-character and 1,000-layout-entry hard limits. The gateway applies bounded token buckets for commands (10/s, burst 10) and presence (30/s, burst 5), validates bounded presence identifiers/collections, and removes both buckets on disconnect/delete. Sessions are capped at 512; dedupe is bounded to 512 entries with a 10-minute TTL; reconnect TTL is 5 seconds; rate-bucket state is capped at 4,096 entries. All defaults are documented in `.env.example`; invalid origins and non-positive limits fail environment validation. Unit boundaries cover decoder/layout, presence strings/collections, rate refill/cleanup, limit parsing, and session capacity. The real Nest/Fastify/Socket.IO/PostgreSQL suite sends ten invalid command attempts through an authenticated joined socket and verifies the eleventh returns `RATE_LIMITED` without command execution. Backend: 159 tests passed; typecheck, lint, and production build passed.
- Increment 3 Tasks 3.6-3.8 make `editor-store` the single lifecycle source for `connecting`, `joining`, `connected`, `resyncing`, `disconnected`, `auth-required`, and `error`. The status bar announces accessible Saving/Saved/Disconnected/Resynchronizing/Error feedback, and stale join/resource callbacks cannot update a disposed controller. Realtime Save is a disabled persistence status and `save()` rejects any collaboration-required state; local HTTP Save remains intact outside realtime. The central lifecycle blocks all canonical mutation paths, Undo/Redo including keyboard shortcuts, toolbox, inspector, relation and layout controls while keeping selection, pan, zoom, and inspection usable. Frontend: 170 tests passed; typecheck, lint, and production build passed.
- Increment 3 Tasks 3.9-3.11 keep authorized collaborator avatars visible in both desktop and compact app bars, with accessible online/offline/activity presentation. Remote cursor, selection, and editing overlays remain flow-space presentation only, use current React Flow internal coordinates for node decorations, and do not intercept canvas input or replace local selection. The canvas performs one initial presentation fit per project; ResizeObserver measurements, responsive transitions, authoritative command/resource/resync updates, presence, and layout changes do not refit or write domain state. Regression coverage verifies roster desktop/mobile/tablet/desktop transitions, coordinate alignment, remote authoritative installation without command-gate echo, no direct React Flow domain mutation, and existing hydration/duplicate-key/stale-listener/zero-size-host/update-loop protections. Frontend: 172 tests passed; typecheck, lint, and production build passed.
- Final automated validation after browser-acceptance preparation: root test passed with 367 tests (frontend 172, backend 159, UML core 36), including the real PostgreSQL/Socket.IO suite. Root typecheck, lint, and production build passed. Strict change/spec OpenSpec validation passed; `git diff --check` passed with known Windows LF/CRLF warnings. `npm audit --omit=dev` still reports 5 transitive findings (2 moderate, 3 high) whose available remediation requires breaking upgrades, so no audit fix was applied.
- Focused backend verification passed: 28 tests, typecheck, and lint.

## Manual Evidence

User-provided durable realtime acceptance passed for OWNER and EDITOR: join/roster, cursor, node and relationship selection, editing/activity, create, rename, add attribute, move, `ApplyLayout`, Save status, blocked Undo/keyboard shortcuts, viewport preservation, offline/reconnect and a post-reconnect mutation converged without Save or reload. The final Chrome confirmation covers relationship creation and multiplicity update, same-user two-tab arbitration with final-tab offline transition, reopen after durable collaboration, UNRELATED concealment, and denied switching without losing the valid subscription. Real 60-minute expiry and protected-mutation behavior also passed; console had no errors, convergence passed, and blockers were 0. A lowercase-class validation diagnostic was observed as expected.

Browser acceptance preparation for Increment 3 is documented in `docs/puds/use-cases/CU-05-browser-acceptance.md`. Playwright is not installed or adopted as development tooling; manual Chrome remains mandatory. The protocol creates OWNER, EDITOR, UNRELATED, projects, and invitation from the UI, avoids secrets and pre-created data, and covers join, commands, presence, two tabs, no-refit responsive transitions, network recovery, project isolation, deletion-based access loss, and real JWT expiry. This completes Tasks 3.12 and 3.13; human evidence remains required by Tasks 4.5 and 4.6.

## Known Limitations And Next Scope

- All Increment 3 and final verification tasks are complete. The supplied Chrome evidence covers owner/editor collaboration, relationship creation/multiplicity, two-tab arbitration, reopen, UNRELATED concealment, denied switching, recovery, expiration, protected mutations, console, and convergence. The accepted-membership-loss browser subcase has no owner UI and remains deterministic-integration-only; no direct database mutation is accepted as manual evidence.
- Tasks 1.6, 1.8, and 1.13 are complete.
- The active design is single-process only; distributed realtime coordination remains future work.

## Result

CU-05 is implementation-complete, manually accepted, and ready for final verification, archive, commit, and push.
