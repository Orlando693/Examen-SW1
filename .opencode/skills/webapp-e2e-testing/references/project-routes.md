# Project Routes

Currently known routes:

- `/`
- `/editor`
- Backend health endpoint: `/health`

Use only routes that exist in the current implementation. Update this reference gradually as new use cases add real routes; do not anticipate CU-03 or later routes here.

For a scenario, identify whether it needs only the frontend route or both the frontend and backend health/API services before starting servers.
