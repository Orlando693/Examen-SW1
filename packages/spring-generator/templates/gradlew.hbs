#!/bin/sh
# Generated wrapper launcher. gradle-wrapper.jar is intentionally not synthesized.
DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
exec java -jar "$DIR/gradle/wrapper/gradle-wrapper.jar" "$@"
