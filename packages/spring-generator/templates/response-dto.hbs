package {{packageName}}.api.dto;

import java.math.BigDecimal;
import java.time.Instant;
import java.time.LocalDate;
{{#each fields}}{{#if enumType}}import {{../packageName}}.domain.{{enumType}};
{{/if}}{{/each}}

public record {{className}}Response(
{{#each fields}}    {{#if enumType}}{{enumType}}{{else}}{{javaType}}{{/if}} {{name}}{{#unless @last}},{{/unless}}
{{/each}}) {
}
