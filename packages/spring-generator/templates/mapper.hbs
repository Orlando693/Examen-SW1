package {{packageName}}.application;

import {{packageName}}.api.dto.{{className}}Response;
import {{packageName}}.domain.{{className}};
import org.springframework.stereotype.Component;

@Component
public class {{className}}Mapper {
    public {{className}}Response toResponse({{className}} entity) {
        throw new UnsupportedOperationException("Mapping implementation is generated in the CRUD increment.");
    }
}
